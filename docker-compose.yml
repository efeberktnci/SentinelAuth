services:
  sentinel:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SENTINEL_SECRET=your_secret_key_here
      - FLASK_SECRET=your_flask_secret_here
    restart: unless-stopped
